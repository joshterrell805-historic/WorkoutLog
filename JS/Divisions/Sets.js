var Sets = new Class({
   Extends: DivisionSuite,

   initialize: function(options) {
      this.parent(options);
      //  just incase anything different ends up being done, or at least for
      //  abstraction's sake, use a new class.
   }
});
